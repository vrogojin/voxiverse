# Godot 4 editor/import cache — regenerated by an `--import` pass, never commit.
.godot/

# Exported web builds live here (produced by scripts/export-web.sh).
/build/

# Do NOT ignore assets/: the generated grass.png is a real committed asset.
